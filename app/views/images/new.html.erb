<h1>Upload Image</h1>

<%= form_with(model: @image, url: images_path, method: :post, multipart: true) do |form| %>
  <div>
    <%= form.label :image %>
    <%= form.file_field :image, accept: 'image/*' %>
  </div>
  <div>
    <%= form.submit 'Upload Image' %>
  </div>
<% end %>
